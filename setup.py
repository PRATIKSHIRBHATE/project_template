from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(), #same as name- ['src']
    version='0.1.0',
    description='this is the project to test how cookiecutter works',
    install_requires = [], #external packages as dependencies
    author='pratik',
    license='',
)
